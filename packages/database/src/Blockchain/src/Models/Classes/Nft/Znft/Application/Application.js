import { Znft } from "../Znft";

export class Application extends Znft{
  constructor(
    name,
    description,
    image,
    external_url
    ) {
    super(
      name,
      description,
      image,
      external_url
    )
  }
}