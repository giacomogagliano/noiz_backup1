import {
  DefaultTheme,
  StyledComponent,
} from "styled-components";
import { BooleanDisplay } from "../../global";

export type LoadingIconType = StyledComponent<
  "div",
  DefaultTheme,
  BooleanDisplay,
  never
>;
