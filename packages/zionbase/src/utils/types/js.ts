import { IcalculateBits } from "../js/lib/calculateBits";
import { IformatTime } from "../js/lib/formatTime";

export interface js {
  lib: {
    calculateBits: IcalculateBits;
    formatTime: IformatTime;
  };
}
